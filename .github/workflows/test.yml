on:
  - push
  - pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        with:
          file_glob: ./**/*.yml
          spectral_ruleset: ./test/.spectral.yml
